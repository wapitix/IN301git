#include <stdio.h>
#include <stdlib.h>
#include "sequence.h"
#include "distance.h"


int main(int argc, char *argv[]) {
  char buffer[64];
  fgets(buffer, sizeof(buffer), f);

  SEQUENCE *S=NULL;
  SEQUENCE *current=NULL;
  SEQUENCE *deb=NULL;
  SEQUENCE *fin=NULL;

  return 0;
}
